<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!--
        Lets Settings → Background reliability prompt the user to whitelist
        Mangako from Doze / App Standby. Without this, periodic scans and
        upload retries can be deferred for hours when the device is idle,
        which is the difference between getting an Inbox notification at
        download-time vs. the next time the user opens the app.
    -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- For legacy storage pre-SAF fallback; SAF Uri is the preferred path -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <!--
        OPTIONAL: 'All files access'. Without it, we fall back to SAF +
        WorkManager content-uri triggers + scan-on-resume + 15-min periodic.
        With it, we can register inotify-backed FileObservers on the watched
        directories' real filesystem paths and notice new .cbz files within
        a second of the writer closing them — which is what every other
        cloud-sync app on Android (Nextcloud, Syncthing, etc.) actually
        does. Users grant it on demand from Settings → Real-time watching;
        the app behaves correctly when it's denied or revoked.
    -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:name=".MangakoApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mangako"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Mangako">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Custom WorkManager initializer (disable default, use Hilt) -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <receiver
            android:name=".work.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!--
            PendingActionReceiver is only ever targeted by explicit intents from
            within the app (the notification's Process / Ignore action buttons),
            so it needs no intent-filter.
        -->
        <receiver
            android:name=".work.notify.PendingActionReceiver"
            android:enabled="true"
            android:exported="false" />
    </application>
</manifest>
